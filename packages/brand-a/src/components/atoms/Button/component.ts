import { Button, ButtonProps } from '@mediamonks/common';
import { colors } from 'core';

Button.defaultProps = {
  color: colors.variants.Text.White,
  backgroundColor: 'red',
};

export { Button, ButtonProps };
