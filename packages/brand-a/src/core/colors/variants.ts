export enum Brand {
  Black = '#000',
  White = '#fff',
}

export enum Element {
  White = '#fff',
  Black = '#000',
}

export enum Text {
  White = '#fff',
  Black = '#000',
}

export type AnyColor = Brand | Element;
