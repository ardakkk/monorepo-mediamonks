import { Footer, FooterProps } from '@mediamonks/common';
import { colors } from 'core';

Footer.defaultProps = {
  color: colors.variants.Text.White,
  backgroundColor: colors.variants.Brand.Purple,
};

export { Footer, FooterProps };
